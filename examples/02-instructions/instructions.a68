print ("2 instructions must be separated by ';'. ");
print (("Printing more than one thing requires two parenthesis. ", "newline is a predefined string to go to new line.", newline));
print (("The last instruction must not have the ';' separator', newline))